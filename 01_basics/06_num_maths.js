const balance = new Number(1000)
console.log(new Number)

toString(balance)// it will return the string representation of the number

console.log(balance.toString().length)

const num = 10.5
console.log(num.toPrecision(3)) // it will return the number in 3 significant digits
console.log(num.toFixed(2)) // it will return the number in 2 decimal places
console.log(num.toExponential(2)) // it will return the number in exponential form with;


//++++++++++++++++++MATHS+++++++++++++++++++++++++++++++
num1= 10 
console.log(Math.abs(num1)) // it will return the absolute value of the number
console.log(Math.round(num1)) // it will return the number rounded to the nearest integer;
console.log(Math.ceil(num1)) // it will return the number rounded up to the nearest integer
console.log(Math.floor(num1)) // it will return the number rounded down to the nearest integer
console.log(Math.round(num1)) // it will return the number rounded to the nearest integer
console.log(Math.trunc(num1)) // it will return the integer part of the number
console.log(Math.sign(num1)) // it will return the sign of the number
console.log(Math.sqrt(num1)) // it will return the square root of the number
console.log(Math.cbrt(num1)) // it will return the cube root of the number
console.log(Math.pow(num1, 2)) // it will return the number raised to the power of 2
console.log(Math.min(1, 2, 3, 4, 5)) // it will return the minimum value from the given numbers
console.log(Math.max(1, 2, 3, 4, 5)) // it will return the maximum value from the given numbers
console.log(Math.random()) // it will return a random number between 0 and 1
console.log(Math.random() * 10) // it will return a random number between 0 and 10
console.log(Math.random() * 10 + 1) // it will return a random number between 1 and 10
